import { backendUsersCommonInterfaces } from './backend-users-common-interfaces';

describe('backendUsersCommonInterfaces', () => {
  it('should work', () => {
    expect(backendUsersCommonInterfaces()).toEqual(
      'backend-users-common-interfaces'
    );
  });
});
