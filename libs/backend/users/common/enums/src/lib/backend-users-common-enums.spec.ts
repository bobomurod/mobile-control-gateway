import { backendUsersCommonEnums } from './backend-users-common-enums';

describe('backendUsersCommonEnums', () => {
  it('should work', () => {
    expect(backendUsersCommonEnums()).toEqual('backend-users-common-enums');
  });
});
