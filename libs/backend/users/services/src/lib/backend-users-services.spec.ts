import { backendUsersServices } from './backend-users-services';

describe('backendUsersServices', () => {
  it('should work', () => {
    expect(backendUsersServices()).toEqual('backend-users-services');
  });
});
