import { backendUsersBackendInterceptors } from './backend-users-backend-interceptors';

describe('backendUsersBackendInterceptors', () => {
  it('should work', () => {
    expect(backendUsersBackendInterceptors()).toEqual(
      'backend-users-backend-interceptors'
    );
  });
});
