import { backendUsersBackendInterfaces } from './backend-users-backend-interfaces';

describe('backendUsersBackendInterfaces', () => {
  it('should work', () => {
    expect(backendUsersBackendInterfaces()).toEqual(
      'backend-users-backend-interfaces'
    );
  });
});
