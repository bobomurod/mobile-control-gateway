import { backendUsersBackendDecorators } from './backend-users-backend-decorators';

describe('backendUsersBackendDecorators', () => {
  it('should work', () => {
    expect(backendUsersBackendDecorators()).toEqual(
      'backend-users-backend-decorators'
    );
  });
});
